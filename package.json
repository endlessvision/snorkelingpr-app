{
  "name": "snorkeling-pr",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "mobile": "pnpm --filter mobile start",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "~5.6.3"
  }
}
